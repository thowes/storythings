add:
	git add .

cim:
	git_projekti_cim

clone:
	git clone https://github.com/thowes/storythings.git

commit:
	git commit

info:
	@git remote show origin

orig:
	@git remote show origin

pull:
	git pull

push:
	git push

repo:
	@git remote -v

st:
	@git status

upd:
	git pull

	

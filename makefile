add:
	git add .

clone:
	git clone https://github.com/thowes/storythings.git

commit:
	git commit

info:
	@git remote show origin

orig:
	@git remote show origin

pull:
	git pull

push:
	git push

repo:
	@git remote -v

st:
	@git status

tls:
	@todo.sh ls +sth

upd:
	git pull

<% provide(:title, @item.name) %>
<h1><%= @item.name %></h1>

 <% if @item.root? %>
  <%= render 'items/show_root' %>
 <% else %>
  <% if @item.is_a_box %>
   <%= render 'items/show_box' %>
  <% else %>
   <%= render 'items/show_item' %>
  <% end %>
 <% end %>

<% provide(:title, 'New Box') %>
<h1>New Box</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@box) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Create New Box", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
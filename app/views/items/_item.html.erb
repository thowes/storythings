<% if item.root? %>
 <li>COLLECTION <b><%= link_to item.name, item %></b></li>
<% else %>
 <% if item.is_a_box %>
  <li>BOX <b><%= link_to item.name, item %></b></li>
 <% else %>
  <li>ITEM <b><%= link_to item.name, item %></b></li>
 <% end %>
<% end %>